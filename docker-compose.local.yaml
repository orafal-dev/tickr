# Optional: publish ports on the host for local development.
# Usage: docker compose -f docker-compose.yaml -f docker-compose.local.yaml up
services:
  postgres:
    ports:
      - "5432:5432"
  api:
    ports:
      - "3001:3001"
  web:
    ports:
      - "3000:3000"
